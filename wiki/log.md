# Operation Log

<!-- Append-only. Newest first. -->

## 2026-07-15

* **Schema**: Reconciled the two parallel sessions spawned by the design-type brain dump — the merged design-surface separation was missing the other session's phasing model, and the two had ruled oppositely on typing. Dossiers now carry a required phasing skeleton on live (`draft`/`active`) dossiers — `phases/phase-1.md`, `phases/later.md` (deferred design scope), `obligations.md` (induced external work; the species, not the "follow-up" genus) — with `phases/` a complete partition of the design's scope (phase-1 + phase-N + later = everything in `design.md`), empty concerns stated rather than inferred, and `design.md` stripped of its Obligations section to stay a pure target specification. Per owner ruling ("pages must have types"), dossier files carry designed types from a vocabulary **scoped to the design surface** (`design | phase | roadmap | alternative | weighing`, defined in `schema/design/dossier.md`, never valid on wiki pages); the wiki registry is untouched at eight types. At terminal states the whole dossier freezes, phase documents retained. Changes: `schema/design/dossier.md`, `schema/wiki/lint.md`, `schema/wiki/page-types/registry.md` (cross-reference), `schema/structure.md`, `scripts/lint.py` (slot/type agreement, live-skeleton checks, phase status enum), `AGENTS.md`, `seed/agents-md-template.md`. Rationale in [[wiki/llm-wiki/decisions/dossier-phasing-and-types]]; [[wiki/llm-wiki/decisions/separate-design-surface]] amended in place.
* **Update**: All ten dossiers gained `type: design` (mechanical schema conformance, frozen dossiers included); the seven live dossiers received backfilled skeletons — [[designs/wiki-federation-and-inheritance]]'s carries real deferred scope (Phase 2 pilot instantiation, Phase 3 first upgrade) and real obligations (the invariant-page candidates); the rest state their empty concerns explicitly. Carried the unmerged session's provenance into the record: verbatim raw captures `design-as-directory-brain-dump`, `design-as-directory-adversarial-review`, `design-scoped-types-and-naming`, plus the reconciliation ruling `design-phasing-reconciliation` (all `raw/chats/`), and the source-capture [[wiki/llm-wiki/sources/design-as-directory-brain-dump]] (routing updated to the reconciled landing places). The unmerged session's wiki-frame artifacts (a `roadmap` registry type, `design/phase` under a wiki parent type, a `finalized` status) were not carried — obviated by the surface split, as recorded in the decision.
* **Schema**: Separated the design surface from the wiki. `schema/` split into `schema/wiki/` (page format, page types, conventions, ingest, lint) and `schema/design/` (the new `dossier.md` spec), with `schema/structure.md` kept at the root as the repo-wide layout. The `design` page type left the registry (eight types remain; authored tier is now decision + invariant); designs are lifecycle-governed dossiers (`draft | active | implemented | superseded | abandoned`) organized by containment, with native `alternatives/` + `weighing.md` fan-outs and a dossier-root-only linking rule from wiki pages. `scripts/lint.py` checks dossier integrity and the reference-direction rule; `scripts/movelog.sh` and `scripts/qmd-index.sh` learned the new surface; `AGENTS.md`, `wiki-manifest.yaml` (`design_surface: true`), `seed/`, and `.kiro/` updated. Rationale in [[wiki/llm-wiki/decisions/separate-design-surface]].
* **Move**: All nine design pages migrated to `designs/{slug}/design.md` dossiers with tombstones in `wiki/moves.log` and repo-wide link retargeting; `wiki/index.md` dropped its Designs sections in favor of the new `designs/index.md` catalog. Epistemic frontmatter (`type`, `novelty`, `confidence`) removed from designs; lifecycle statuses assigned (active: wiki-self-experimentation, wiki-federation-and-inheritance, knowledge-surfacing-design, agent-context-portability; implemented: evidence-tier-schema; superseded: trellis-repo-design; draft: the three project-wiki designs).
* **Ingest**: Processed [Claude Code Thread: Design Alternatives and the Design-Surface Separation](wiki/llm-wiki/sources/design-surface-separation-claude-code-thread.md) from this session (declared-condensed raw in `raw/chats/`, owner statements verbatim). Created [[wiki/llm-wiki/decisions/separate-design-surface]] (authored tier, user-instigated) and the first dossier [[designs/design-surface]]; deprecated [[wiki/llm-wiki/decisions/designs-die-into-decisions]] (superseded, lifecycle rule repealed). No interpretive pages promoted: the claim-shaped/commitment-shaped distinction is stated normatively in `schema/` and the promotion test does not pass for a wiki-side duplicate.
* **Roadmap**: Added the design-surface meta-experiment line (revisit after the next 2–3 fan-outs), retired the superseded design-lifecycle-rule line (superseded before its trigger fired, not verdicted), and opened a Design Surface question section (wiki graduation, scoped types, weighing shape).

## 2026-07-13

* **Schema**: Adopted the instigator-tier model — who creates which page types and when — and restaged ingest into bounded capture (Stage 1: source-isolated, delegable to a context-free capture agent, ends at the source-capture with routing candidates) plus vault-aware review and routing (Stage 2: discussion, promotion decisions per the tiers, contradiction detection). Changes: `schema/wiki/page-types/registry.md` (Instigator Tiers section + spec-frontmatter convention), minimal frontmatter and Instigation lines across all nine `schema/wiki/page-types/` specs, `schema/wiki/ingest.md` (two-stage workflow), `scripts/lint.py` + `schema/wiki/lint.md` (composition stats — observable, never a target), and compact-contract updates in `AGENTS.md` and `seed/agents-md-template.md`. Rationale in [[wiki/llm-wiki/decisions/staged-ingest-and-instigator-tiers]].
* **Ingest**: Processed [Claude Code Thread: Staged Ingest and Instigator Tiers](wiki/llm-wiki/sources/staged-ingest-instigator-tiers-claude-code-thread.md) from this session (declared-condensed raw in `raw/chats/`, owner statements verbatim). Created [[wiki/llm-wiki/decisions/staged-ingest-and-instigator-tiers]] (authored tier, user-instigated). No interpretive pages promoted: the thread's concepts land as schema operating rules, canonical in `schema/`, and the promotion test does not pass for wiki-side duplicates.
* **Roadmap**: Added the instigator-tiers meta-experiment line (revisit on downstream friction data plus pilot instantiation), the downstream-friction-reports self-maintenance item, and open questions on middle-tier instigator blurriness and spec-frontmatter scope; marked the first-upgrade inheritance exercise armed by this schema change.

## 2026-07-12

* **Schema**: Added the design-lifecycle rule to `schema/wiki/page-types/design.md` — a blueprint design dies into `decision` page(s) when its artifact ships; only operative or unbuilt content stays live. Rationale in [[wiki/llm-wiki/decisions/designs-die-into-decisions]], adopted from the a-priori review of all nine design pages.
* **Create**: First application of the lifecycle rule — three decision pages extracted from the federation blueprints: [[wiki/llm-wiki/decisions/adopt-single-goal-federation]] (the founding split decision with all rejected alternatives, including the first-draft rails), [[wiki/llm-wiki/decisions/trellis-repo-bootstrap]] (name, two-surface boundary, seeded-not-inherited AGENTS.md, migration mechanics), and [[wiki/llm-wiki/decisions/designs-die-into-decisions]] (the doctrine itself). [[designs/trellis-repo-design]] deprecated to a routing stub; [[designs/wiki-federation-and-inheritance]] slimmed to the live design (status → stable) with normative content deferred to `schema/`.
* **Update**: Design-review fixes across the rest of the set — [[designs/project-wiki-application-guide]] rescoped to content curation and the forked promotion loop (practice lessons → ai-research, method lessons → here; instance creation belongs to `seed/interview.md`); [[designs/project-wiki-template]] reconciled with the schema-inheritance model (coordination meta-files exempt from the type system like the root journals, workflow/eval pages mapped to registry types, domain/architecture as topics — adopted pending pilot validation); [[designs/multi-agent-project-wiki-pattern]] now cites the 2026-07-08 parallel-merge collision as its first (negative) operational evidence; [[designs/wiki-self-experimentation]] gained a Federation Scope section (method lines here, instance-local lines in instances, friction harvested upstream); [[designs/evidence-tier-schema]] shed its restatement of `schema/wiki/page-format.md` rules, keeping rationale only; [[designs/agent-context-portability]] lost its schema-invalid `aka` field and gained a dated-content caution on the capability matrix; [[designs/knowledge-surfacing-design]] self-references date-attributed to the pre-split vault. Executed by three parallel subagent sessions with disjoint file ownership; lint 0 errors.
* **Bootstrap**: Repository created by executing the bootstrap plan in [[designs/trellis-repo-design]]. All initial content arrived from `pnw/ai-research` at commit `3c5908840f2fb13a543ebb602b4acfaa84ae96a5` (branch `claude/wiki-assessment-critique-uzse03`): the `wiki/llm-wiki/` and `wiki/agent-context/` topics (76 pages), their cited raw sources (35 files plus PDF siblings), `raw/repos/` operational snapshots, and the schema/scripts/adapters layer. Cross-repo copies do not preserve git history; this entry is the provenance record, and `ai-research`'s `wiki/moves.log` tombstones every departed page with a `moved-to trellis::` forwarding address.
* **Schema**: Granulated `schema/wiki/page-types.md` into `schema/wiki/page-types/` — one standalone normative file per type plus `registry.md` (vocabulary, location rules, extensibility, decision tree). Schema files carry no rationale back-references; substantial change rationale goes to `decision` pages, discovered by archaeology.
* **Schema**: Ratified the cross-wiki independence rule in `schema/wiki/page-format.md` (owner decision, 2026-07-12): citing a peer wiki's source-capture imports its `evidence` tier at face value, but wikis operated by the same owner and agents count as one source for independence — no cross-repo self-corroboration. Documented peer-link syntax and outbound-only responsibility in `schema/wiki/conventions.md`.
* **Create**: Wrote the distribution layer — `seed/interview.md` (instantiation interview), `seed/manifest-template.yaml`, `seed/agents-md-template.md`, `seed/pages/` skeletons — plus `wiki-manifest.yaml` (upstream form, no `upstream:` block), `scripts/movelog.sh`, and this repo's `AGENTS.md`/adapter files. Updated `scripts/lint.py` to skip `::` peer links and exclude `episodes.md` from graph metrics.
* **Create**: Wrote [[wiki/overview]] (states the single goal and the two health axes), assembled [[wiki/index]] from the migrated topics, and carried the method-side backlog and meta-experiment log into [[wiki/roadmap]].
